name: Import and test

on:
  push:
  pull_request:

jobs:
  import-and-test:
    name: Headless import + gdUnit4
    runs-on: ubuntu-24.04
    env:
      GODOT_VERSION: "4.7.2"
      GODOT_CACHE_DIR: ${{ github.workspace }}/.godot-ci
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Restore Godot cache
        id: godot-cache
        uses: actions/cache@v4
        with:
          path: ${{ env.GODOT_CACHE_DIR }}
          key: godot-${{ env.GODOT_VERSION }}-linux.x86_64

      - name: Resolve Godot 4.7.2
        id: godot
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p "${GODOT_CACHE_DIR}"

          mark_found() {
            local candidate="$1"
            echo "godot_bin=${candidate}" >> "${GITHUB_OUTPUT}"
            echo "found=true" >> "${GITHUB_OUTPUT}"
            echo "Using Godot at ${candidate}"
            "${candidate}" --version || true
          }

          if [ -x "${GODOT_CACHE_DIR}/godot" ]; then
            mark_found "${GODOT_CACHE_DIR}/godot"
            exit 0
          fi

          if command -v godot >/dev/null 2>&1; then
            mark_found "$(command -v godot)"
            exit 0
          fi

          if command -v godot4 >/dev/null 2>&1; then
            mark_found "$(command -v godot4)"
            exit 0
          fi

          echo "Godot ${GODOT_VERSION} missing from CI cache and PATH; attempting download."
          archive="${GODOT_CACHE_DIR}/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          url="https://github.com/godotengine/godot-builds/releases/download/${GODOT_VERSION}-stable/Godot_v${GODOT_VERSION}-stable_linux.x86_64.zip"
          if curl -fsSL -o "${archive}" "${url}"; then
            unzip -qo "${archive}" -d "${GODOT_CACHE_DIR}"
            bin="$(find "${GODOT_CACHE_DIR}" -maxdepth 2 -type f -name 'Godot_v*-linux.x86_64' | head -n 1)"
            if [ -n "${bin}" ]; then
              chmod +x "${bin}"
              ln -sfn "${bin}" "${GODOT_CACHE_DIR}/godot"
              mark_found "${GODOT_CACHE_DIR}/godot"
              exit 0
            fi
          else
            echo "Download failed."
          fi

          echo "Godot binary missing in CI cache; skipping import and tests."
          echo "found=false" >> "${GITHUB_OUTPUT}"

      - name: Run Python autoload tests
        run: python3 -m unittest discover -s tests/unit -p 'test_*.py'

      - name: Run Python honor-meter tests
        run: python3 tests/unit/test_honor_state.py

      - name: Import character JSON
        run: python3 tools/import_characters.py

      - name: Run Python loyalty tests
        run: python3 tests/unit/test_loyalty.py

      - name: Run Python save HMAC tests
        run: python3 tests/unit/test_save_hmac.py

      - name: Run Python feed / camp-night tests
        run: python3 tests/unit/test_feed.py

      - name: Run Python controller tests
        run: python3 tests/unit/test_cid_controller.py

      - name: Run Python a1_vivar tests
        run: python3 tests/unit/test_a1_vivar.py

      - name: Validate chapter graph
        run: python3 tools/validate_graph.py

      - name: Run Python chapter-graph tests
        run: python3 tests/unit/test_tagus_join.py

      - name: Run Python a1_cardena tests
        run: python3 tests/unit/test_a1_cardena.py

      - name: Run Python a1_navapalos tests
        run: python3 tests/unit/test_a1_navapalos.py

      - name: Run Python a1_castejon tests
        run: python3 tests/unit/test_a1_castejon.py

      - name: Run Python a1_alcocer tests
        run: python3 tests/unit/test_a1_alcocer.py

      - name: Run Python booty-divide tests
        run: python3 tests/unit/test_booty_divide.py

      - name: Run Python embassy1 tests
        run: python3 tests/unit/test_embassy1.py

      - name: Run Python Poyo hub tests
        run: python3 tests/unit/test_poyo_hub.py

      - name: Run Python a1_tevar tests
        run: python3 tests/unit/test_a1_tevar.py

      - name: Run Python a2 siege tests
        run: python3 tests/unit/test_a2_siege.py

      - name: Run Python a2 Jerónimo hub tests
        run: python3 tests/unit/test_a2_jeronimo.py

      - name: Run Python embassy2 tests
        run: python3 tests/unit/test_embassy2.py
      - name: Run Python a2 Yusuf tests
        run: python3 tests/unit/test_a2_yusuf.py

      - name: Run Python embassy3 tests
        run: python3 tests/unit/test_embassy3.py

      - name: Run Python a2 repay Raquel tests
        run: python3 tests/unit/test_a2_repay_raquel.py

      - name: Run Python a2 Tagus tests
        run: python3 tests/unit/test_a2_tagus.py

      - name: Run Python a2 bodas tests
        run: python3 tests/unit/test_a2_bodas.py

      - name: Run Python a3_leon tests
        run: python3 tests/unit/test_a3_leon.py

      - name: Run Python a3_bucar tests
        run: python3 tests/unit/test_a3_bucar.py
      - name: Run Python a3_despedida tests
        run: python3 tests/unit/test_a3_despedida.py

      - name: Run Python a3_corpes tests
        run: python3 tests/unit/test_a3_corpes.py

      - name: Run Python a3_querella tests
        run: python3 tests/unit/test_a3_querella.py

      - name: Run Python a3_toledo tests
        run: python3 tests/unit/test_a3_toledo.py

      - name: Run Python Valencia wait tests
        run: python3 tests/unit/test_a3_valencia_wait.py

      - name: Run Python DuelResolver tests
        run: python3 tests/unit/test_duel_resolver.py

      - name: Run Python Pentecost tests
        run: python3 tests/unit/test_a3_pentecost.py

      - name: Run Python combat tests
        run: python3 tests/unit/test_combat.py

      - name: Run Python horse companion tests
        run: python3 tests/unit/test_horse_companion.py

      - name: Import project
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --import

      - name: Run EventBus headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_event_bus.gd

      - name: Run HonorState headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_honor_state.gd

      - name: Run loyalty headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_loyalty.gd

      - name: Run SaveService headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_save_hmac.gd

      - name: Run camp-night headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_camp_night.gd

      - name: Run CidController headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_cid_controller.gd

      - name: Run TouchHud headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_touch_hud.gd

      - name: Run DialogueBridge headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_dialogue_bridge.gd

      - name: Run a1_vivar prologue headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a1_vivar.gd

      - name: Run Tagus AND-join headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_tagus_join.gd

      - name: Run a1_cardena farewell headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a1_cardena.gd

      - name: Run a1_navapalos sleep headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a1_navapalos.gd

      - name: Run a1_castejon dawn-take headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a1_castejon.gd

      - name: Run a1_alcocer dawn-sortie headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a1_alcocer.gd

      - name: Run booty-divide headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_booty_divide.gd

      - name: Run embassy1 GiftToKing headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_embassy1.gd

      - name: Run combat headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_combat.gd

      - name: Run horse companion headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_horse_companion.gd

      - name: Run Poyo hub headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_poyo_hub.gd

      - name: Run a1_tevar table/Colada headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a1_tevar.gd

      - name: Run a2 siege clock headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a2_siege.gd

      - name: Run a2 Jerónimo hub headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a2_jeronimo.gd

      - name: Run embassy2 Avengalvón escort headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_embassy2.gd

      - name: Run a2 Yusuf headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a2_yusuf.gd

      - name: Run Python embassy3 tests (post-import)
        if: steps.godot.outputs.found == 'true'
        run: python3 tests/unit/test_embassy3.py

      - name: Run embassy3 GiftToKing headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_embassy3.gd

      - name: Run a2 repay Raquel headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a2_repay_raquel.gd

      - name: Run a2 Tagus headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a2_tagus.gd

      - name: Run a2 bodas headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a2_bodas.gd

      - name: Run a3_leon lion scene headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_leon.gd

      - name: Run a3_bucar shore/Tizona headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_bucar.gd
      - name: Run a3_despedida departure/Avengalvón headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_despedida.gd

      - name: Run a3_corpes aftermath headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_corpes.gd

      - name: Run SpeechTrial headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_speech_trial.gd

      - name: Run a3_querella SpeechTrial headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_querella.gd

      - name: Run a3_toledo Cortes headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_toledo.gd

      - name: Run Valencia wait headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_valencia_wait.gd

      - name: Run DuelResolver headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_duel_resolver.gd

      - name: Run spectator lists headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_carrion.gd

      - name: Run Pentecost headless test
        if: steps.godot.outputs.found == 'true'
        run: |
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s res://tests/unit/test_a3_pentecost.gd

      - name: Run gdUnit4
        if: steps.godot.outputs.found == 'true'
        run: |
          if [ ! -f addons/gdUnit4/plugin.cfg ]; then
            echo "gdUnit4 not vendored yet; skipping gdUnit4."
            exit 0
          fi
          cmd=""
          for candidate in \
            addons/gdUnit4/bin/GdUnitCmdTool.gd \
            addons/gdUnit4/bin/GdUnitCmdLineTool.gd; do
            if [ -f "${candidate}" ]; then
              cmd="${candidate}"
              break
            fi
          done
          if [ -z "${cmd}" ]; then
            echo "gdUnit4 command tool missing; skipping tests."
            exit 0
          fi
          "${{ steps.godot.outputs.godot_bin }}" --headless --path . --audio-driver Dummy -s "${cmd}" -- --add res://tests --continue

      - name: Skip notice
        if: steps.godot.outputs.found != 'true'
        run: echo "Godot binary missing in CI cache; import, EventBus/loyalty/CidController/Tagus headless tests, and gdUnit4 skipped."
